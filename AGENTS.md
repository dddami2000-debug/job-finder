[RULES-OK]

## 만드는 것

- 충남대학교 자연과학 및 공학·IT 계열의 4학년 학생이 인턴과 신입 공고를 찾는 화면
- 대상 직무는 연구·개발·설계
- 우선 관심 분야는 안테나공학
- 신입 공고도 보여주되 인턴 공고를 더 우선함
- 인턴 근무 기간은 지원 여부와 추천 순위에 영향을 주지 않음

## 화면

- 공고 카드에서 지원 자격을 가장 먼저 판단할 수 있게 함
- 카드에 `경력 1년 필수`처럼 중요한 자격 조건을 한 줄로 표시함
- 지원 자격 원문이 데이터에 없으면 조건을 추측하지 않고 `원문 확인 필요`라고 표시함
- 처음에는 공고를 간단한 카드로 표시함
- 카드를 누르면 같은 자리에서 주요 업무와 전체 지원 조건을 펼침
- 관심 없는 공고에는 `넘기기` 버튼을 표시함
- `넘기기`를 누르면 다음 공고를 같은 자리에 표시함
- 넘긴 공고는 별도의 `넘긴 공고` 목록에서 확인하게 함
- 담은 공고는 로그인 없는 수업용 공용 목록으로 Supabase에 저장함
- 펼쳐진 카드를 다시 누르면 같은 자리에서 접히게 함
- 흰색과 짙은 녹색을 중심으로 장식하고 시스템 산세리프 글꼴을 사용함
- 휴대폰 화면을 우선하고 PC에서는 같은 내용을 더 넓게 표시함

## 추천 규칙

- 해외 근무 공고를 제외함
- 관련 경력 1년 이상이 필수인 공고를 제외함
- 연구·개발·설계 분야의 인턴과 신입 공고만 대상으로 함
- 남은 공고는 지원 마감일이 가까운 순서로 정렬함
- 마감일이 같으면 신입보다 인턴 공고를 먼저 표시함
- 위 조건이 같으면 안테나공학 관련 공고를 먼저 표시함
- 제목·직무·주요 업무에 `안테나`, `전자파`, `RF`, `무선`, `전파`, `위성통신`이 있으면 안테나공학 키워드 공고로 판정함
- 정규직 전환, 급여 공개, 시작일 조정, 기숙사 및 교통비 지원은 순위에 반영하지 않음
- 기업 규모와 인지도는 추천 순위에 반영하지 않음
- 국내 근무 지역은 별도 우선순위를 두지 않고 사용자가 입력한 지역으로 필터링함

## 평가 기준

- 공고 목록을 열면 각 카드의 핵심 지원 자격이 한 줄로 보임
- 공고 카드를 누르면 같은 자리에서 주요 업무와 전체 지원 조건이 보임
- `넘기기`를 누르면 해당 공고가 사라지고 다음 공고가 같은 자리에 보임
- `넘긴 공고`를 누르면 이전에 넘긴 공고 목록이 보임
- 추천 목록에는 해외 근무 공고와 관련 경력 1년 이상 필수 공고가 보이지 않음
- `마감 임박 순`을 선택하면 지원 마감일이 가까운 공고부터 보임
- 마감일이 같은 인턴과 신입 공고가 있으면 인턴 공고가 먼저 보임

## 이번에 안 만드는 것

- 회원가입과 로그인
- 지원서 작성과 실제 입사 지원 제출
- 마감 알림과 이메일·문자 알림
- 기업 인지도·급여·복지 비교 기능
- 넘긴 공고의 기기 간 동기화

## 작업 규칙

- 공고 카드의 첫 화면에서 핵심 지원 자격을 한 줄로 확인할 수 있게 한다
- 공고 카드를 누르면 주요 업무와 전체 지원 조건이 같은 자리에서 펼쳐지게 한다
- `넘기기`를 누르면 다음 공고가 같은 자리에 나타나게 한다
- `넘긴 공고`를 누르면 이전에 넘긴 공고를 다시 확인할 수 있게 한다
- 추천에서 제외할 공고가 추천 목록에 나타나지 않게 한다
- 휴대폰과 PC 화면에서 글자와 버튼이 잘리거나 겹치지 않게 한다
- 비밀번호, 비밀키, service_role 키와 토큰을 코드나 공개 파일에 적지 않는다
- 담당자 이름, 연락처와 이메일 같은 개인정보를 저장하거나 공개하지 않는다
